<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roadsideassistance">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoadSideAssistance">
        <activity android:name=".Users.UserHome"></activity>
        <activity android:name=".Common.PutForgettOtp" />
        <activity android:name=".Common.ForgettPassword" />
        <activity android:name=".Common.PutOtp" />
        <activity android:name=".Common.PhoneNumber" />
        <activity android:name=".Common.Login" />
        <activity android:name=".Users.SignupUser" />
        <activity android:name=".Provider.SignupProvider" />
        <activity android:name=".Common.Identity" />
        <activity
            android:name=".Common.SplashAcreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>